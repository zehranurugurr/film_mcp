startCommand:
  type: stdio
  configSchema:
    # JSON Schema defining the configuration options for the MCP.
    type: object
    properties: {}
  commandFunction:
    |-
    (config) => ({ command: 'python', args: ['-m', 'server'] })
  exampleConfig: {}